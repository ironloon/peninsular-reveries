# Quick Task 260401-12s: Fix dev server MIME type errors and convert puzzle creator to modal overlay

## Plan 1: Fix dev server and convert puzzle creator to modal

### Task 1: Fix serve command

**Files:** `package.json`

**Action:** Change `"serve": "serve public"` to `"serve": "serve dist"` so the dev server serves the esbuild output directory where compiled JS actually lives.

**Verify:** `npm run serve` starts without errors; browser loads game JS modules correctly (no MIME type errors in console).

**Done:** Dev server serves from `dist/` and game loads without module errors.

---

### Task 2: Convert puzzle creator from details/summary to modal overlay

**Files:** `public/super-word/index.html`, `public/super-word/game.css`, `src/super-word/renderer.ts`

**Action:**

**HTML (`public/super-word/index.html`):**
- Replace the `<details id="puzzle-creator">` block (lines 41–63) with:
  - A button `<button id="puzzle-creator-btn" class="btn puzzle-creator-btn">🛠️ Puzzle Creator</button>` on the start screen
  - A new modal overlay `<div id="puzzle-creator-modal" class="puzzle-creator-modal" hidden>` outside the start screen (sibling to screens), containing:
    - A `.puzzle-creator-content` wrapper (similar to `.celebration-content` pattern)
    - A close button `<button id="puzzle-creator-close" class="btn puzzle-creator-close">✕</button>`
    - The same form body (labels, inputs, suggestions div, options row, play button) currently inside `.puzzle-creator-body`

**CSS (`public/super-word/game.css`):**
- Remove `overflow-y: auto;` from `#start-screen` (line 93)
- Remove the `.puzzle-creator` details-related styles: `.puzzle-creator` border/padding/margin, `.puzzle-creator-toggle`, `.puzzle-creator-toggle::-webkit-details-marker`, `.puzzle-creator[open] .puzzle-creator-toggle` (lines 135–163)
- Add `.puzzle-creator-btn` style—small, semi-transparent button similar to `.puzzle-creator-toggle` opacity/font
- Add `.puzzle-creator-modal` overlay styles matching `.celebration-popup` pattern: `position: fixed; inset: 0; z-index: 150; display: flex; align-items: center; justify-content: center; background: rgba(0,0,0,0.35)`
- Add `.puzzle-creator-modal[hidden] { display: none; }`
- Add `.puzzle-creator-content` styled like `.celebration-content` but with the start-screen purple gradient
- Add `.puzzle-creator-close` absolutely positioned top-right inside content

**TypeScript (`src/super-word/renderer.ts`):**
- In `renderPuzzleCreator()`, replace the `details.open = false` line with hiding the modal: `modal.hidden = true`
- Add modal open/close wiring:
  - `puzzle-creator-btn` click → `modal.hidden = false`
  - `puzzle-creator-close` click → `modal.hidden = true`
  - Click on backdrop (modal element itself, not content) → `modal.hidden = true`
  - Escape key → `modal.hidden = true` (add keydown listener)

**Verify:** Game starts, start screen has no scrollbar. "Puzzle Creator" button opens a centered modal overlay. Typing words shows suggestions. Clicking Play starts the game and closes modal. Escape and close button dismiss modal. Clicking backdrop dismisses modal.

**Done:** Puzzle creator is a modal overlay; start screen has no overflow scroll; all puzzle creator functionality preserved.
